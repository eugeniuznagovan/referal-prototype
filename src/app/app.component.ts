import { Component } from '@angular/core';
import { RouterOutlet } from '@angular/router';

@Component({
  selector: 'app-root',
  template: `
    {{ title }}
  `,
  styles: ``
})
export class AppComponent {
  title = 'referal';
}
